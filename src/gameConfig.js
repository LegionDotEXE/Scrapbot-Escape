// GameConfig.js   

// Gameplay constants — Initially just a placeholder

const gameConfig = {
  playerSpeed: 200,
  enemySpawnRate: 2000,
  obstacleSpeed: 150,
  scoreIncrement: 10
};

export default gameConfig;